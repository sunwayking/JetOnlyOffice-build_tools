# JetOnlyOffice

JetOnlyOffice 是一个长期维护的 ONLYOFFICE 衍生产品。本词汇表固定产品、源码与发布讨论中的核心术语，避免把研究材料或混合产物误称为正式产品能力。

## Language

**完整 Fork**:
由 JetOnlyOffice 控制、能够独立生成全部发布产物的一组 ONLYOFFICE 源码仓库。正式功能必须进入这组源码的可追溯历史。
_Avoid_: 混合镜像、bundle 注入方案、移动补丁包

**上游**:
JetOnlyOffice 用于同步、比较和升级的 ONLYOFFICE 原始源码来源，不直接代表 JetOnlyOffice 的可发布产品状态。
_Avoid_: 产品源码、发布分支

**发布基线**:
一组经过兼容性确认、不可变且能够共同生成同一产品版本的源码修订。
_Avoid_: 最新版、浮动分支、近似版本

**Source lock**:
将发布基线落实为机器可读的仓库提交、源码树和依赖摘要清单，是构建系统允许使用的唯一源码输入。
_Avoid_: branch、tag、构建时最新提交

**首版发布架构**:
首版正式服务端发布物必须支持的 CPU 与操作系统组合，即 `linux/amd64`；其他架构不承担首版门禁。
_Avoid_: Android ABI、浏览器平台、计划中的 arm64

**首版发布物集合**:
由同一 source lock 生成并共同发布的 DEB、完整运行目录、OCI 镜像、对应源码归档、SBOM、许可证与 NOTICE、摘要和构建证明。
_Avoid_: 原生移动应用、Desktop Editors、缺少对应源码的二进制

**正式构建环境**:
由 Windows 本机通过 Docker Desktop 编排、在按摘要锁定的 Ubuntu 24.04 Linux/amd64 容器中执行的受控构建环境，是首版正式产物的唯一编译来源。
_Avoid_: Windows 原生交叉工具链、直接使用 WSL 发行版、开发机任意环境

**两阶段构建**:
联网 bootstrap 只按 source lock 下载并校验源码与依赖缓存，断网 build 只消费这些带摘要的输入生成正式产物。
_Avoid_: 编译时拉取、浮动包索引、未校验缓存

**二进制可重复构建**:
同一 source lock 在独立干净目录中生成的 DEB、运行目录归档和 OCI 镜像具有完全相同 SHA-256 的构建性质。
_Avoid_: 仅功能相同、忽略时间戳差异、同一目录重复打包

**离线镜像组装**:
以摘要锁定的 Ubuntu 24.04 基础镜像、本次构建的 DEB 和 bootstrap 缓存的运行依赖生成 OCI 镜像，组装阶段不得访问外网。
_Avoid_: 下载官方 DEB、在线 apt、浮动基础镜像

**官方兼容运行契约**:
首版镜像默认可按 Community 单镜像方式启动，同时保留既有环境变量连接外部 PostgreSQL、Redis 和 RabbitMQ 的部署接口。
_Avoid_: 强制多容器拓扑、JetOnlyOffice 私有环境变量、破坏性部署改造

**正式部署 JWT**:
正式镜像默认启用且必须由部署方提供有效外部密钥的文档服务鉴权；缺失、空值或占位密钥都会阻止启动。
_Avoid_: 内置共享密钥、静默禁用 JWT、开发临时密钥

**JetOnlyOffice 品牌**:
面向用户和发布渠道使用的产品名称、标识及元数据；ONLYOFFICE 名称只保留在上游来源、许可归属和兼容性所需内部标识中。
_Avoid_: 冒用 ONLYOFFICE 产品身份、删除上游归属、无必要改名内部接口

**完整对应源码**:
能够在正式构建环境中重建某次二进制发布的全部源码、修改、构建与打包脚本、source lock、许可证、NOTICE 和依赖信息集合。
_Avoid_: 仅主仓库链接、不含构建脚本、与产物版本不一致的源码

**Mobile Web 编辑器**:
JetOnlyOffice 面向触控设备提供的文档、电子表格、演示文稿和 PDF 编辑器族。只有四类编辑器都满足各自的完整编辑、保存和重开验收时，才能称为完整的 Mobile Web 编辑功能。
_Avoid_: 单编辑器 PoC、桌面界面缩放版、PDF 仅查看或批注

**正式验收端**:
决定某项 Mobile Web 能力能否被标记为完成的真实设备运行环境。模拟器和桌面浏览器的设备模拟只提供开发反馈，不构成正式验收证据。
_Avoid_: 开发预览、设备模拟

**手机布局**:
用于手机横竖屏完整编辑的触控优先信息架构，由主工具栏、快捷栏和全宽二级面板组成，并随方向重排而不改变命令语义。
_Avoid_: Desktop 布局缩小版、平板布局

**Desktop 布局**:
用于桌面和手机以外的大屏设备的完整功能区布局；平板横竖屏均使用该布局。
_Avoid_: 手机二级面板布局

**二级面板**:
手机布局中承载文本、段落、插入和对象属性等任务的触控操作面。常用任务保留画布上下文，高级任务进入全屏页面。
_Avoid_: Desktop 右侧栏、级联菜单、通用弹窗

**完整编辑**:
同一发布基线中 Desktop Web 支持且当前权限与文件格式允许的内置编辑能力，在手机布局中都具有可触达的触控入口；第三方插件、宏和外部连接器不属于首版完成范围。
_Avoid_: 常用功能子集、截图功能集、桌面界面复制

**PDF 完整编辑**:
同一发布基线 Desktop PDF Editor 支持的文本、对象、页面、批注、绘图、表单和签名等内置能力，在 Mobile 中具有等价触控入口、保存和重开语义。
_Avoid_: PDF 查看器、仅批注、仅表单填写

**PDF 一级任务空间**:
PDF 手机布局中直接可达的六类编辑任务，即编辑、插入、批注、页面、表单和签名；绘图属于批注，对象属性随当前选区打开，搜索、协作和设置保持为全局入口。
_Avoid_: 全部收入更多菜单、按 Desktop 功能区原样缩放、将全局命令混入编辑任务

**外观签名**:
以上传图片、手写或键入方式放入 PDF 的可见签名外观，不包含证书验证，也不独立证明签署人身份或文件完整性。
_Avoid_: 数字签名、证书签名、已验证签名

**证书数字签名**:
使用证书对 PDF 内容进行密码学签署并可验证签署人、完整性和签名后变更状态的签名；没有可信验证结果时只能标记为未验证。
_Avoid_: 外观签名、签名图片、手写批注

**永久脱敏**:
从 PDF 保存结果中不可逆地移除指定文字、图像或区域的底层内容，使其在重开后无法被搜索、复制或恢复；仅用黑色形状遮挡不属于脱敏。
_Avoid_: 涂黑外观、黑色矩形、隐藏图层

**扩展能力**:
由第三方插件、宏或外部连接器提供且不属于编辑器内置命令集的可选能力，其 API 与 Desktop 行为保持兼容，但 Mobile UI 需要独立验收。
_Avoid_: 内置编辑命令、首版完整编辑门禁、默认触控适配

**命令搜索**:
手机布局中按名称查找当前选择对象、文件格式和权限允许的内置命令与设置，并导航到其正常二级面板或高级页面的统一入口。
_Avoid_: 绕过权限直接执行、插件搜索、唯一命令入口

**画布长按**:
编辑画布和对象区域中由 Mobile adapter 接管、根据当前选择显示触控上下文菜单的手势；标准表单控件仍使用浏览器原生选择与复制行为。
_Avoid_: 全局禁用原生菜单、桌面右键事件、长按直接执行命令

**返回层级**:
Android 返回键、系统手势和浏览器返回共同遵循的逐层退出顺序：软键盘、临时浮层、功能面板、编辑器，并在最终离开前保护未确认保存内容。
_Avoid_: 直接退出文档、各入口不同顺序、关闭键盘即导航

**旋转连续性**:
手机横竖屏切换时保持文档位置、缩放、选区、对象选择和面板任务，只清除无法可靠重新锚定的软键盘与临时浮层。
_Avoid_: 重载编辑器、重置任务、保留错误锚定菜单

**输入法组合**:
中文拼音等输入法尚未确认的临时文本状态，不进入协同广播或撤销栈；用户确认组合后才作为一次完整编辑提交。
_Avoid_: 每个 composition update 都提交、半成品协同文本、拆分撤销记录

**宿主中立 Mobile Web**:
仅通过标准 Web API 使用文件、相机、剪贴板、下载和分享能力的编辑器，不依赖某个 Android 应用或 JetTaskFlow 的私有 JavaScript Bridge。
_Avoid_: 应用专属桥接、仅 WebView 可用、Chrome 行为分叉

**图片输入来源**:
Mobile Web 插入图片时统一支持的设备图库或文件、相机、HTTPS 链接和剪贴板四类标准 Web 输入渠道。
_Avoid_: 私有原生桥接、仅本地文件、权限失败后丢失编辑状态

**命令覆盖清单**:
逐项关联 Desktop 内置命令、适用上下文、权限条件、Mobile 入口和自动化测试的机器可读发布基线。
_Avoid_: 人工功能列表、仅截图入口、无测试映射

**权限画像**:
用于验证同一命令在完整编辑、仅审阅、仅评论、仅表单填写和只读会话中可见性、禁用状态与执行结果的标准权限配置。
_Avoid_: 仅测试管理员、只隐藏按钮、Mobile 独立权限规则

**首版格式门禁**:
要求 `DOCX/ODT`、`XLSX/ODS`、`PPTX/ODP` 和 `PDF` 完成打开、完整编辑、保存及重开的正式验收集合；旧二进制 Office 格式通过转换进入编辑。
_Avoid_: 仅 OOXML、旧格式原样往返、PDF 只读

**Mobile 源码基座**:
发布基线中已有的文档、电子表格和演示文稿 Mobile 编辑结构；JetOnlyOffice 在其上统一产品行为和交互，而不是另建一套相互隔离的移动编辑器。
_Avoid_: 从零重写、原样发布上游 Mobile、Euro-Office bundle

**保存状态机**:
Desktop 与 Mobile 共同使用的编辑持久化状态，包括自动保存、显式保存、服务端确认和版本冲突；只有服务端确认完成的内容才称为已保存。
_Avoid_: Mobile 本地保存队列、仅前端显示成功、未确认保存

**可重试保存失败**:
由超时、临时服务异常或确认延迟造成且共享保存状态机允许继续重试的保存失败；编辑保持可用，但界面持续标记内容尚未保存并提供重试与保存副本。
_Avoid_: 已保存、静默重试、阻断编辑

**阻断保存失败**:
由权限撤销、身份过期、会话失效或服务端明确拒绝造成、继续修改会扩大丢失风险的保存失败；会话冻结新修改并进入恢复流程。
_Avoid_: 临时超时、继续编辑、自动丢弃

**编辑会话状态层**:
四类编辑器共同使用的打开、可编辑、重连、未保存、保存失败、版本冲突和致命错误状态语义及恢复入口；具体编辑器只提供上下文，不自行发明错误状态。
_Avoid_: 各编辑器独立弹窗、静默失败、仅记录日志

**真实阶段式加载**:
按连接服务、下载文件、解析内容、渲染首屏和初始化协作展示实际打开阶段的全屏状态；只有底层提供真实测量值时才显示百分比，超过性能预算只标记为加载较慢而不伪装成失败。
_Avoid_: 模拟百分比、无限旋转图标、超时即宣告失败

**冲突恢复**:
共享保存状态机无法自动核对或合并服务端版本时保护当前内容的阻断流程，优先保存副本或重试，只有用户二次确认后才可放弃当前内容并加载服务器版本。
_Avoid_: 自动覆盖、静默重载、默认丢弃本地内容

**致命错误恢复**:
编辑器无法继续安全运行时保护当前进程内内容的全屏流程，优先导出可用副本，再提供重试、重新打开和返回；不得通过自动刷新隐式丢弃内容。
_Avoid_: 自动刷新、只显示关闭按钮、向用户暴露原始堆栈

**必要运维遥测**:
为自托管实例的可靠性与性能排障记录的最小化结构化事件，只包含阶段耗时、运行环境类别、文件类型、命令编号、会话状态、错误码和关联编号，不包含文档内容或可直接识别用户的数据。
_Avoid_: 产品使用分析、文档内容采集、第三方分析服务

**临时断网**:
编辑会话失去服务端连接但页面或 WebView 进程仍存活的状态；确认断线后冻结新修改，保留当前进程内未确认内容及安全的查看、复制和副本导出能力，完成服务端状态核对后才恢复编辑。
_Avoid_: 离线模式、本地编辑队列、断线期间继续修改

**完整协作**:
同一发布基线中 Desktop 已支持且当前权限允许的实时共同编辑、评论与回复、提及和修订处理能力，在 Mobile 中具有等价的触控入口和状态语义。
_Avoid_: 仅显示协作者、只读评论、Mobile 独立协作状态

**触控验收门槛**:
手机布局必须满足的真机操作下限：主要触控目标至少为 `44 × 44 CSS px`、相邻目标间距至少为 `8 CSS px`，上下文菜单不遮挡当前选择对象，并可连续操作选区手柄和软键盘。
_Avoid_: 仅视觉相似、依赖像素级精确点击、桌面悬停替代

**Mobile UI 无障碍基线**:
工具栏、面板、菜单、对话框和状态提示遵循 `WCAG 2.2 AA`，包括可访问名称、可见焦点、非颜色单一表达和 `200%` 界面文本缩放下的完整命令可达性。
_Avoid_: 仅屏幕阅读器补丁、仅颜色状态、固定字号布局

**性能基线机**:
Mobile Web 首版性能门禁所使用的最低档真机，固定为 Xiaomi `2409BRN2CC`（`pond`、MediaTek MT6769、约 3.56 GiB RAM、720×1640），而不是随测试人员变化的任意当前设备。
_Avoid_: 当前手机、模拟器、仅高端参考机

**Mobile 性能预算**:
性能基线机在局域网、Release 构建和静态资源已缓存条件下必须满足的交互上限，包括打开时间、命令反馈、连续手势帧率和最长冻结时间。
_Avoid_: 主观流畅、桌面浏览器测速、只测启动页

**标准性能语料库**:
版本化的 `DOCX`、`XLSX`、`PPTX` 和 `PDF` 性能测试输入，固定内容类型、规模和文件大小，只用于重复测量 Mobile 性能预算，不代替完整功能覆盖。
_Avoid_: 临时样例文档、功能验收全集、任意用户文件

**首版完整翻译语言**:
JetOnlyOffice 新增 Mobile 文案在首版必须完整翻译和真机验证的语言集合，即简体中文和英语；上游已有语言资源继续保留。
_Avoid_: 删除其他语言、所有语言都已完整验证、机器翻译即完成

**Mobile 主题模式**:
共享同一布局和语义颜色令牌的跟随系统、浅色和深色三种显示模式，用户可以手动覆盖系统选择。
_Avoid_: 独立深色布局、仅反色处理、不可覆盖的系统主题

**Android 兼容目标**:
首版计划保持功能兼容的 Android 10–16 系统范围；它通过自动化回归验证，不等同于性能基线机或发布阻断真机。
_Avoid_: 最低性能档、所有版本真机验收、仅 Android 16 可运行

**Android 发布阻断端**:
性能基线机 Android 16 上的 Chrome 和 System WebView；任一端未通过均不得发布 Mobile Web 首版。
_Avoid_: 模拟器、桌面设备模拟、非阻断兼容回归

**iPhone 兼容目标**:
iOS 16+ Safari 与 WKWebView 上非阻断的 Mobile 兼容范围，沿用 Android 的信息架构和视觉语言，仅适配安全区、软键盘及浏览器差异。
_Avoid_: iOS 专属界面、首版发布阻断端、未经验证的完整支持

**平板发布阻断端**:
至少一台视口短边不小于 `600 CSS px` 的 Android 真机平板，必须在横竖屏、Chrome 和 System WebView 中以 Desktop 布局通过四类编辑器完整回归；具体设备最迟在正式平板回归开始前锁定并记录软硬件指纹。
_Avoid_: 手机放大模式、响应式模拟器、Mobile 手机布局

**Desktop 无回归门禁**:
共享编辑能力层变更相对发布基线不得破坏四类 Desktop 编辑器的打开、编辑、保存、协作和重开行为，并须通过上游测试及 Chromium、Firefox、WebKit 自动化回归。
_Avoid_: 仅 Mobile 测试通过、人工抽查、允许已知 Desktop 退化

**发布证据清单**:
以单一发布 ID 绑定 source lock、源码提交、构建环境、产物摘要、SBOM、测试、真机与性能证据及许可证检查的机器可读事实；缺少任一必需证据都不能形成协调发布。
_Avoid_: 分散报告、人工勾选、缺失证据仍标记通过

**协调发布**:
四个产品 fork 由同一 source lock 共同提升到 `main` 并使用一致产品版本标签的发布动作，单个仓库不能脱离其余仓库独立声明正式版本。
_Avoid_: 单仓发布、浮动依赖、仅分支名一致
