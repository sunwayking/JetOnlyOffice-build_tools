# 生产环境只默认启用最小化自托管运维遥测

生产环境默认启用自托管的必要运维日志，只记录阶段耗时、设备与浏览器类别、编辑器和文件类型、命令编号、会话状态变化、错误码及关联编号；严禁记录文档内容、选区文字、评论、文件名、带令牌 URL、剪贴板、证书材料和原始用户标识。产品使用分析默认关闭，数据不得发送到第三方分析服务；用户主动生成诊断包时，必须先展示将包含的数据范围并取得仅针对本次导出的明确同意。
