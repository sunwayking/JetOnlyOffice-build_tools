---
status: superseded by ADR-0053
---

# 建立版本化 Mobile 性能语料库

首版性能门禁使用固定且版本化的三类输入：不超过 `10 MB`、50 页并含表格、20 张图片、评论和修订的 `DOCX`；不超过 `10 MB`、8 个工作表、共 5 万个非空单元格并含公式、筛选、条件格式和图表的 `XLSX`；不超过 `15 MB`、40 页并含图片、形状、表格、图表、切换和动画的 `PPTX`。该语料库只用于验证性能预算，完整命令覆盖由独立功能测试文档负责。
