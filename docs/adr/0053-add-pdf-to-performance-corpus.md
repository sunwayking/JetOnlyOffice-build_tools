# 标准性能语料库扩展为四类文件

首版性能语料库在 ADR-0018 的 `DOCX`、`XLSX` 和 `PPTX` 输入之外增加一个不超过 `15 MB`、60 页的 `PDF`，其中包含可编辑文字、20 张图片、表格、批注与绘图、表单字段和未签署的签名字段，并适用相同的打开及交互性能预算。签名字段保持未签署，避免性能测试中的编辑行为天然使已有数字签名失效；本决策取代 ADR-0018 的三类语料范围。
