# 首版正式验收端为 Android WebView 和 Android Chrome

JetOnlyOffice Mobile Web 首版以 Android WebView 和 Android Chrome 真机作为正式验收端，桌面浏览器的移动设备模拟仅用于开发测试。iPhone Safari 不阻塞首版完成，但后续 iPhone 界面必须与 Android 使用同一信息架构、命令层级和视觉语言，避免形成独立的 iOS 产品分支。
