# 首版区分外观签名与证书数字签名

首版完整支持 PDF 外观签名和签名表单字段，但不创建新的证书数字签名；已有证书数字签名只展示底层引擎实际返回的验证状态，无法可信验证时必须显示“未验证”，并在编辑可能改变已签署内容前提示签名状态可能失效、保存后重新取得验证状态。同版本 Desktop Web 明确禁用证书签名支持，而外观签名仅提供上传、手写和键入，因此该边界不缩减 Desktop Web 可用内置命令范围；浏览器端证书创建推迟到独立的 Web PKI 和证书提供方设计完成之后。
