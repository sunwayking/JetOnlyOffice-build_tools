# 固定首版 Mobile 性能预算

在性能基线机、局域网、Release 构建和静态资源已缓存的条件下，标准验收文档从打开到可编辑不得超过 `8 秒`，工具栏、二级面板和直接格式命令的视觉反馈应满足 `P95 ≤ 250 毫秒`，连续滚动与缩放的帧率中位数不得低于 `45 FPS`，且不得出现超过 `1 秒`的界面冻结或崩溃。标准验收文档作为独立的版本化测试输入固定，避免用不同内容解释性能结果。
